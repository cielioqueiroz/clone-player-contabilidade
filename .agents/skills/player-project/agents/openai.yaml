interface:
  display_name: "Player Project"
  short_description: "Build and release the Player concept website"
  default_prompt: "Use $player-project to complete the next documented milestone, verify its behavior, and update the project status."
policy:
  allow_implicit_invocation: true
